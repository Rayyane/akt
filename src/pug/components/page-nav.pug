.page-nav#page_nav
  nav.section-nav
    .scrollnav
      ul
        each item, index in menu.fashion
          li(style=`top: ${30 * (menu.fashion.length - index)}px`)
            a.tooltipped(
              href='#'+item.name
              data-position="left"
              data-tooltip=$t.fashion_landing['header_'+item.name]
            )
  .scrollnav
    |
    |
    a.btn-floating.btn-large.primary.tooltipped.waves-effect.waves-light(
      href="#home"
      data-position="left"
      data-tooltip="To Top"
    )
      .icon.material-icons arrow_upward
    |
    |
